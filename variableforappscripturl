//This is the formatted name for Quick Paste on the URL

APPSCRIPT URL - ?timestamp={{timestamp}}&nama={{formData.nama}}&nomor_hp={{formData.nomor_hp}}&email={{formData.email}}&provinsi={{formData.provinsi}}&kota={{formData.kota}}&agen={{formData.agen}}&alasan={{formData.alasan}}&lama_konsumsi={{formData.lama_konsumsi}}&kritik_saran={{formData.kritik_saran}}&testimoni={{formData.testimoni}}&SKU={{skuName}}

This is the code for timestamp UTC +07:00, put it on timestamp variable


//TIMESTAMP

function() {
    var now = new Date();
    var utc7Offset = 7 * 60 * 60 * 1000; // 7 hours in milliseconds
    var utc7Time = new Date(now.getTime() + utc7Offset);

    // Format the time as ISO string, but without the 'Z' at the end since it's not UTC anymore
    var isoString = utc7Time.toISOString().split('.')[0]; // Remove milliseconds
    return isoString + '+07:00';
}

//ENDTIMESTAMP
